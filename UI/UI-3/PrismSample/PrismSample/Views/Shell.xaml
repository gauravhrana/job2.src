<Window x:Class="PrismSample.Views.Shell"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PrismSample"
        mc:Ignorable="d"
        xmlns:prism="http://www.codeplex.com/prism"
        Title="Prism Sample App" Height="600" Width="600"
        Icon="/Resources/Icon/App.ico">
    <Grid>
        <Grid.RowDefinitions>
            <!--<RowDefinition Height="Auto"/>-->
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>        
        
        <!--<StackPanel Orientation="Horizontal">
            <Button Content="Client List" Margin="5" Command="{Binding NavigateCommand}" CommandParameter="ViewListClient"/>
            <Button Content="Add Client" Margin="5" Command="{Binding NavigateCommand}" CommandParameter="ViewSaveClient"/>
        </StackPanel>-->
        <!--<Menu prism:RegionManager.RegionName="MenuRegion" Grid.Row="0">
        </Menu>-->
        <DockPanel>
            <ContentControl DockPanel.Dock="Top" prism:RegionManager.RegionName="MainNavigationRegion" Grid.Row="0" />
        </DockPanel>
        
        <ContentControl prism:RegionManager.RegionName="MainContentRegion" Grid.Row="1" />
    </Grid>
</Window>
